using UnityEngine;

public class dasdasda : MonoBehaviour
{
    dasdasda script;
    public float velocidad = 10.10f;
    public int vida = 9;
    public GameObject carta;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Cascanueces()
    {
        script.Cascanueces();
    }
}
